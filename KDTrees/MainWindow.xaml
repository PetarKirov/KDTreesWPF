<Window x:Class="KDTrees.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:KDTrees"
        Title="MainWindow" Width="800" Height="400">
    
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <local:DrawingCanvas Grid.Column="0"
                             x:Name="drawingCanvas"
                             Background="#338800AA"/>
        
        <ListBox Grid.Column="1"
                 x:Name="log"
                 ItemsSource="{Binding LogStream}"/>
        
        <StackPanel Grid.Row="1" Grid.ColumnSpan="2">
            <Button Content="Fill with random points" HorizontalAlignment="Center" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
